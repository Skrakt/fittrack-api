
name: ci-fittrack
 
on:
  pull_request:
    branches:
      - main
 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "build OK — ${{ github.actor }} sur ${{ github.ref }}"
 
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test
        run: echo "test OK"